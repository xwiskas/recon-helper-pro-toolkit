"""The local dashboard: a loopback-only web view over the same core engine.

Nothing in this package performs recon. Every route calls
:class:`~recon_helper_pro.core.engine.Engine`, exactly as the CLI does.
"""
